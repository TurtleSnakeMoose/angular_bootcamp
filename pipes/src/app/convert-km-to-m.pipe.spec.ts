import { ConvertKmToMPipe } from './convert-km-to-m.pipe';

describe('ConvertKmToMPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertKmToMPipe();
    expect(pipe).toBeTruthy();
  });
});
